import { createGlobalStyle } from 'styled-components';

const FontStyles = createGlobalStyle`
@font-face {
  font-family: 'Ideal Sans';
  src: url('fonts/ideal-sans.ttf');
}
`;

export default FontStyles;
